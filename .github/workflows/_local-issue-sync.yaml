name: Issues

on: [issues]

jobs:
  update:
    name: Update Issue
    uses: bencekov/grafana-k8s-operator/.github/workflows/issues.yaml@test-implement-oauth-interface
    secrets: inherit
    with:
      component: observability
